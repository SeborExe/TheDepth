public enum Scenes
{
    LoadingScene,
    Outside,
    Dungeon
}

public enum DestinationIdentifier
{
    A, B, C, D, E, F, G, H
}

public enum CharacterClass
{
    Player,
    Goblin_Mele
}

public enum Stat
{
    Health,
    ExperienceReward,
    ExperienceToLevelUp,
}